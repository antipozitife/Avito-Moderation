# Avito-like Advertisement Management Platform

Фронтенд-приложение для управления объявлениями с модерацией и аналитикой. Приложение предоставляет интуитивный интерфейс для просмотра, фильтрации и модерации объявлений на основе готового API сервера.

## 🎯 Особенности

- **Просмотр объявлений** с расширенной фильтрацией по статусу, категории и цене
- **Система модерации** для одобрения, отклонения или запроса доработки объявлений
- **Аналитика и статистика** с визуализацией через графики и диаграммы
- **Сортировка** по дате создания, цене и приоритету
- **Пагинация** для удобного навигации по результатам
- **Поиск** в реальном времени по названиям объявлений

## 📋 Требования

- **Node.js** v20 или выше
- **npm** или **yarn** в качестве пакетного менеджера
- Запущенный локальный сервер API на порту `3001` (см. папку `server`)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка подключения к API

Приложение уже настроено на подключение к серверу через `proxy` в `package.json`:

```json
"proxy": "http://localhost:3001"
```

Убедитесь, что сервер запущен и доступен на этом адресе перед запуском фронтенда.

### 3. Запуск приложения в режиме разработки

```bash
npm start
```

Приложение откроется в браузере по адресу `http://localhost:3000` и будет автоматически перезагружаться при изменении файлов.

### 4. Запуск сервера

В отдельном терминале перейдите в папку `server` и запустите API:

```bash
cd server
npm install
npm start
```

Сервер запустится на порту `3001`.

## 🏗️ Архитектура проекта

```
src/
├── components/           # Переиспользуемые компоненты
│   ├── Card/            # Карточка объявления
│   └── Filters/         # Компонент фильтрации
├── pages/               # Страницы приложения
│   ├── MainPage/        # Главная страница со списком объявлений
│   ├── ModerationPage/  # Страница модерации
│   └── StatsPage/       # Страница аналитики
├── app.tsx              # Главное приложение с маршрутизацией
├── index.tsx            # Точка входа
└── index.html           # HTML-шаблон
```

## 🔧 Технологический стек

### Обоснование выбора технологий

**React v18+** — Современный фреймворк для создания интерактивных UI с эффективной компонентной архитектурой и встроенной системой состояния. Обеспечивает высокую производительность и удобство разработки благодаря хукам и функциональным компонентам.

**react-router-dom v7** — Стандартная библиотека для маршрутизации в React приложениях. Обеспечивает связь между страницами без перезагрузки (SPA), поддерживает параметры в URL и навигацию программным способом через `useNavigate`.

**Node.js v20** — Среда выполнения JavaScript с LTS поддержкой, обеспечивающая стабильность и безопасность. Позволяет использовать единственный язык (JavaScript) для фронтенда и управления зависимостями через npm.

**TypeScript** — Добавляет статическую типизацию к JavaScript, что улучшает надежность кода, облегчает рефакторинг и предоставляет лучшую поддержку IDE. Особенно полезен при работе с компонентами React и пропсами.

**Axios** — Простая и надежная HTTP-библиотека для взаимодействия с API. Автоматически обрабатывает JSON, поддерживает интерцепторы и имеет лучшую обработку ошибок по сравнению с fetch.

**Recharts** — Компонентная библиотека графиков и диаграмм на базе React. Предоставляет готовые компоненты для визуализации данных (PieChart, BarChart) без необходимости работать с Canvas напрямую.

**Chakra UI** — Высокоуровневая библиотека компонентов UI с встроенной доступностью и теминацией. Ускоряет разработку интерфейса благодаря готовым стилизованным компонентам.

## 📁 Структура папок

- `src/components` — Переиспользуемые компоненты (Card, Filters)
- `src/pages` — Основные страницы приложения
- `src/styles` — CSS файлы для стилизации компонентов и страниц
- `public` — Статические файлы (иконки, изображения)
- `server` — Папка с готовым Backend API (не входит в фронтенд код)